# grpc-web-html
## Start Client
`$ phyton -m simpleHTTPServer 8081`  
loacalhost:8081 をブラウザーで起動その後clientのディレクトリーに行くとサンプルが起動します。
## Start Server
`$ docker-compose up`
## Build Client
`$ cd client`
`$ npm install`
## Build Server
`$ cd server`
`$ npm install`
